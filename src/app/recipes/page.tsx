import { Metadata } from 'next'
import React from 'react'

export const metadata: Metadata = {
  title: 'Recipes',
  description: '...',
}

const page = () => {
  return (
    <div>recipes</div>
  )
}

export default page